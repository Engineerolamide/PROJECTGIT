{
	"name": "SQL script 1000004",
	"properties": {
		"content": {
			"query": "-- sql table\n\nCREATE EXTERNAL DATA SOURCE blob_retail_datasource\nWITH (\n    LOCATION = 'https://storagedatalakeengineer.blob.core.windows.net/projectmedalion'\n);\n\n\nCREATE EXTERNAL FILE FORMAT ParquetFormat\nWITH (\n    FORMAT_TYPE = PARQUET\n);\n\nCREATE EXTERNAL TABLE daily_revenue (\n    event_date DATE,\n    daily_revenue FLOAT,\n    total_purchases INT\n)\nWITH (\n    LOCATION = '/gold/',  -- Important: folder path only\n    DATA_SOURCE = blob_retail_datasource,\n    FILE_FORMAT = ParquetFormat\n);\n\n\n\nselect * from daily_revenue",
			"metadata": {
				"language": "sql"
			},
			"currentConnection": {
				"databaseName": "buitin",
				"poolName": "Built-in"
			},
			"resultLimit": 5000
		},
		"type": "SqlQuery"
	}
}